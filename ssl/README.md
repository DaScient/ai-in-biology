# Place TLS certificates here (e.g. fullchain.pem, privkey.pem) for use by the
# nginx reverse proxy defined in docker-compose.yml. The directory is mounted at
# /etc/nginx/ssl inside the nginx container. The files themselves are not
# committed to the repository.
